# Sub2API v0.1.170-watch.1 发布与切流交付摘要

## Meta

- 确认包：发布准备包 v1
- 官方基线：Sub2API `v0.1.170`
- 发布分支：`release/0.1.170-watch.1`
- 发布分支提交：`3d2571d7d`
- 交付状态：accepted，生产已切换至绿色，蓝色保留
- 隔离验收地址：`http://127.0.0.1:8094/login`

## 本次完成

已完成定制版的发布分支、冻结依赖构建、`linux/amd64` 不可变镜像、生产数据库与配置备份、隔离绿色验收和 Nginx 蓝绿切流。生产代理已从蓝色 `8093` 切换到绿色 `8094`；故障转移保持关闭，未退款、未改余额，蓝色容器继续保留。

## 关键改动

- 发布分支和功能分支已推送到定制远端；最终镜像为 `sub2api-custom:0.1.170-watch.1-3d2571d7d`。
- 生产备份位于 `/www/wwwroot/sub2api-deploy/backups/20260803T0900Z-pre-0.1.170-watch.1`，数据库 dump、部署配置和 Nginx 配置校验通过。
- 绿色容器 `sub2api-170-green-watch1` 已应用官方 `192/193` 与定制 `312/313` 等增量迁移并保持 healthy。
- 创建独立临时验收管理员；未读取或修改现有生产管理员密码。
- 用户绿色验收通过并明确授权后，远端 `v0.1.170-watch.1` 已纠正到最终提交 `3d2571d7d`。
- 用户明确确认切流后，Nginx 单一代理目标由 `8093` 改为 `8094`；切流前配置备份位于 `/www/wwwroot/sub2api-deploy/backups/20260803T101649Z-nginx-8093-to-8094`。

## 验证结果

- 后端 service/repository/handler/admin 回归通过。
- 前端 207 个 Vitest 文件、1430 项测试、vue-tsc、生产 build 通过。
- Docker `--frozen-lockfile` 安装和 `linux/amd64` 镜像构建通过；运行时自报 `0.1.170-watch.1 / 3d2571d7d`。
- 蓝色与绿色 `/health` 均正常，Nginx 仍指向 `8093`。
- 备份 `SHA256SUMS` 全部通过，PostgreSQL 18 可读取 dump catalog。
- 可见验证已完成登录页、临时管理员登录和强制合规承诺入口；未代替用户接受合规承诺。
- 切流后约一分钟观察到 55 个新增 Nginx 请求全部为 200、98 条新增 usage、0 个 failover attempt、0 个新增 5xx、0 条绿色错误日志；公网 health 持续返回 200。

## 用户选择与原因

用户确认执行发布准备包 v1，完成绿色验收后分别授权标签纠正和生产切流；蓝色容器按原安全边界继续保留，停止蓝色仍需独立确认。

## 复盘结论

发布准备本身已完成且未改变生产流量。构建时发现 pnpm lockfile 缺少 overrides 元数据，修复后重新完成冻结安装与镜像构建；这一修复曾使已推送标签落后于最终发布提交，现已在用户明确授权后用带旧对象校验的受保护强制推送纠正。

## 记忆与进化后续

本次学习保留在任务级：定制 release tag 只能在最终容器构建和冻结依赖验证通过后创建。暂不提出全局规则变更。

## 下一步建议

1. 继续观察绿色的健康、错误率、延迟和计费安全线。
2. 观察稳定后，由用户单独确认是否停止蓝色容器。
3. 启用故障转移、退款或数据库恢复仍保持独立授权。
