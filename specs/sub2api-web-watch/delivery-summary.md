# Sub2API v0.1.170-watch.1 发布准备交付摘要

## Meta

- 确认包：发布准备包 v1
- 官方基线：Sub2API `v0.1.170`
- 发布分支：`release/0.1.170-watch.1`
- 发布分支提交：`3d2571d7d`
- 交付状态：awaiting_user_acceptance
- 隔离验收地址：`http://127.0.0.1:8094/login`

## 本次完成

已完成定制版的发布分支、冻结依赖构建、`linux/amd64` 不可变镜像、生产发布前数据库与配置备份，以及不接生产流量的绿色容器。蓝色 `8093` 继续承载流量，绿色 `8094` 仅用于验收；故障转移保持关闭，未退款、未改余额、未切流、未停止蓝色容器。

## 关键改动

- 发布分支和功能分支已推送到定制远端；最终镜像为 `sub2api-custom:0.1.170-watch.1-3d2571d7d`。
- 生产备份位于 `/www/wwwroot/sub2api-deploy/backups/20260803T0900Z-pre-0.1.170-watch.1`，数据库 dump、部署配置和 Nginx 配置校验通过。
- 绿色容器 `sub2api-170-green-watch1` 已应用官方 `192/193` 与定制 `312/313` 等增量迁移并保持 healthy。
- 创建独立临时验收管理员；未读取或修改现有生产管理员密码。
- 发现远端 `v0.1.170-watch.1` 标签仍指向 lockfile 修复前提交，未在缺少明确授权时强制改写。

## 验证结果

- 后端 service/repository/handler/admin 回归通过。
- 前端 207 个 Vitest 文件、1430 项测试、vue-tsc、生产 build 通过。
- Docker `--frozen-lockfile` 安装和 `linux/amd64` 镜像构建通过；运行时自报 `0.1.170-watch.1 / 3d2571d7d`。
- 蓝色与绿色 `/health` 均正常，Nginx 仍指向 `8093`。
- 备份 `SHA256SUMS` 全部通过，PostgreSQL 18 可读取 dump catalog。
- 可见验证已完成登录页、临时管理员登录和强制合规承诺入口；未代替用户接受合规承诺。

## 用户选择与原因

用户确认执行发布准备包 v1，选择先完成备份、隔离绿色部署和验收入口，再分别授权标签纠正、切流和停蓝，以避免影响正在运行的账号。

## 复盘结论

发布准备本身已完成且未改变生产流量。构建时发现 pnpm lockfile 缺少 overrides 元数据，修复后重新完成冻结安装与镜像构建；这一修复使已推送标签落后于最终发布提交，因此标签纠正必须作为显式动作处理，不能隐藏或擅自强推。

## 记忆与进化后续

本次学习保留在任务级：定制 release tag 只能在最终容器构建和冻结依赖验证通过后创建。暂不提出全局规则变更。

## 下一步建议

1. 用户使用临时管理员登录绿色环境并亲自完成合规确认，验收智能运营页面。
2. 用户明确授权后，将 `v0.1.170-watch.1` 纠正到最终发布提交。
3. 用户验收通过后单独确认 Nginx 切流；切流后继续保留蓝色容器。
4. 停止蓝色、启用故障转移、退款或数据库恢复均保持独立授权。
