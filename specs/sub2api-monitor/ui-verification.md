# UI 验证记录：Sub2API 实时分组监控

## lcodex 新版兼容正式实施包 v1

- 自动化检查：公开根页确认是 Vue 单页应用，直访 `/login` 返回 404；授权实现改为根页加载后使用固定 `/login` 客户端路由，不提交账号或密码。
- 契约检查：根域 `/user/profile`、`/groups/available`、`/groups/rates`、`/channels/available` 均返回 JSON 401；新版前端价格资源使用 `/api/v1/channels/available`。
- 可见验证限制：内置浏览器被站点当前访问策略阻断。请在最新 `release/AIZZZWatch-darwin-arm64/AIZZZWatch.app` 中添加或编辑 lcodex，点击网页授权后检查窗口自动关闭和来源钱包数据。

**日期**: 2026-07-18
**环境**: macOS Electron development runtime；浏览器预览 `127.0.0.1:5188`
**验证方式**: 真实 Electron 窗口可见检查、浏览器预览点击路径与辅助功能树检查

## 已检查路径

1. 主窗口打开后显示两站演示快照、总余额、分组数、健康状态和最近同步时间。
2. 站点选择会更新右侧余额、响应时间、价格能力、分组和管理员账号。
3. 设置按钮打开站点配置；名称、API 地址、网页登录授权、备用 JWT/refresh token、管理员凭据类型和轮询间隔均可达。
4. 设置界面明确说明登录令牌写入 macOS 安全存储，安全输入框不会显示明文。
5. 本轮静态构建与适配层验证通过；真实网页登录路径需要用户提供可测试站点后在授权窗口完成联调，当前未填写任何真实账号信息。
5. 切组入口打开远程变更确认框；站点、账号和目标分组可见。
6. 目标分组没有变化时“确认切换”保持禁用，避免无意义远程写入。
7. 气泡模式显示总余额和健康点，按钮具有“打开 AIZZZWatch 紧凑窗口”可访问名称。
8. 点击气泡可以恢复紧凑窗口；主快照状态未丢失。
9. 浏览器预览中，“网页登录授权”按钮保持禁用，并显示“浏览器预览不执行网页登录授权”的提示。
10. 浏览器预览中手工填写“预览联调站点”和根地址后保存，左侧站点列表立即切换为新增站点，详情标题同步更新，底部状态显示“浏览器预览”。
11. 浏览器预览新增站点的详情区显示“不会连接真实 Sub2API”的提示，不执行真实登录、轮询或远程写入。
12. 桌面打包产物通过本机只读调试确认 `window.aizzz` 存在，`runtime.isBrowserPreview=false`，网页登录授权不再走浏览器预览 API。
13. 本地模拟站点将 `/login` 302 重定向到 `/auth`，`auth:login` 成功返回并写入站点配置，说明 `-3` 中断不再被误判为失败。
14. 本地 cookie-required 模拟站点在 `/login` 下发会话 cookie 后，主进程请求携带 cookie 与 UA，站点可返回健康余额与分组快照。
15. 详情区错误提示按钮语义已调整：授权失效时显示“重新登录”，普通错误时显示“重试”，并分别触发重新授权或当前站点重试。
16. v7 新增：右上角“几秒前”由每秒 tick 驱动，不再只依赖远程刷新触发重绘。
17. v7 新增：左侧中转站增加 `全部` 与 Sub2API `platform` 分类 tabs；选择分类后站点列表、详情分组和低价聚合一起过滤。
18. v7 新增：创建/编辑站点表单增加充值比例 `1:n`；站点列表和详情统计会显示当前比例。
19. v7 新增：低价聚合区按结构化模型价格、分组倍率和站点充值比例计算有效价格，用于展示“谁家的什么模型最便宜”。
20. v8 新增：首页主视角改为分类比价榜，固定分类为 Anthropic、OpenAI、Gemini、Antigravity、Grok、其他，分类 tab 显示当前候选数量。
21. v8 新增：主表列出所有站点/分组/模型候选，不再只显示每个模型的最便宜项；默认按有效成本从低到高排序。
22. v8 新增：站点信息移到右侧“来源钱包”，保留余额、充值比例、健康状态、响应、价格能力和当前分类分组。
23. v3-2 新增：价格榜表头的“倍率 / 充值 / 有效成本”支持点击排序；默认文案会随当前列与方向同步，且在窄屏布局下仍能保持列隐藏规则不乱。
24. v3-2 补充：紧凑模式和窄屏视图下增加独立“排序”胶囊条；表头会退回为静态标签，避免重复可点击控件挤占内容宽度。
25. v4-1 新增：价格榜增加“最终倍率”列，按 `倍率 ÷ 充值比例` 计算并支持点击排序；例如 `0.1 / 10` 与 `0.01 / 1` 都显示为 `0.010x`。
26. v4-2 新增：`我的站点` 管理台分区 tabs 从 overview 里解锁，进入管理视角后始终可见 `总览 / 用户 / 账号 / 分组 / 渠道 / 平台 / 用量 / 设置`；没有可管理站点时先显示空态引导，不再把入口藏起来。
27. v4-4 新增：账号工作台改为“组合”语义，AX 树可见“一个账号可以同时挂多个分组”的说明；账号行的当前分组以组合 chips/多行信息呈现，目标组合的候选将作为增删项而不是单一替换目标。
28. v4-5 新增：使用临时隔离 `AIZZZWATCH_USER_DATA_DIR` 演示数据启动桌面包，通过 Electron CDP 点击“我的站点”后确认 `station-account-focus` 出现、当前账号详情显示 `claude-main`、推荐补入 `Gemini Image 生图`，并有 1 条账号行高亮。
29. v4-6 新增：在聚焦账号卡下方继续显示“更优候选”池，Electron CDP 看到 3 个可点候选分组，分别标记最终倍率，截图证据落盘。
30. v4-7 新增：在同一隔离演示数据下，Electron CDP DOM 读数确认“预算线”输入存在；设置为 `0.068` 时候选恢复到 3 条，设置为 `0.024` 时候选收紧到 1 条，说明预算线过滤生效。
31. v4-8 已完成代码改动：近期分组变化的本地保留上限提升到 120 条，同时把变化弹窗里的后台总览嵌入移除；本轮未再次打开窗口复核，需用户重开后确认观感。
32. v4-9 新增：近期分组变化真实历史不再显示一键清空；删除分组的“清理”只隐藏删除提示并持久化隐藏 ID，不删除该分组历史时间线；历史保留上限提升到 500 条。
33. v4-9 补充：正式 userData 只读摘要与临时实例清理均已完成；公开记录不保留本机文件计数或临时路径，避免干扰用户打开验证。
34. v4-10 新增：顶部工作视角增加“数据中心”，切换后隐藏来源钱包侧栏，显示只读本地数据摘要：数据目录、站点/授权/管理员计数、隐藏分组、手动标签、变化历史、偏好文件和站点文件状态。
35. v4-11 新增：聪明哥只读探针确认 Node `fetch` 对普通接口返回 403 Cloudflare HTML，而 Electron/Chromium `net.fetch` 对 `/user/profile`、`/groups/available`、`/groups/rates`、`/channels/available` 返回 200 JSON；桌面端请求层已切到 Chromium 网络栈。
36. v4-14 新增：浏览器预览进入 `我的站点 → 账号`，默认调度筛选为“调度开启”，只显示开启调度的 `claude-main`，批量推荐数量为 1；切换到“调度关闭”后显示 `openai-pool` 灰态行、调度关闭 badge、“不参与推荐”说明，批量推荐数量为 0，且不存在推荐操作按钮。
37. v4-14 新增：同一预览路径读取 DOM/CSS 确认“当前组合最低”使用 `current-baseline-card`，主值 `0.045x` 直接显示，`white-space=normal`、`overflow=visible`，并显示来源说明 `OpenAI Fast · 当前 1 个分组`。
38. v4-15 新增：浏览器预览进入 `我的站点 → 账号` 后，工具栏出现 `账号渠道筛选` 和 `账号所在分组筛选`；演示账号行两栏布局读取为 `511px / 307px`，右侧包含“当前组合最低/最低候选”内容，不再是空白区。
39. v4-15 新增：账号行显示来源/成本状态按钮；演示账号显示 `未绑来源` 和 `成本未设`，点击状态按钮可进入绑定来源或账号成本弹窗。
40. v4-15 新增：演示账号调整为多分组样本；账号列表读取到 `Claude Code Pro · 0.068x`、`Gemini Image 生图 · 0.023x` 两个完整分组 chip，chip 移除按钮为可用状态。分组筛选选中 `Gemini Image 生图` 后保留 1 行，叠加 `openai` 渠道后正确出现空态。
41. v5 新增：浏览器预览进入 `我的站点 → 账号` 后，聚焦账号卡不再展示“站点余额”；来源/成本改为紧凑 icon tag，账号名、站点摘要、状态、分组、当前组合最低和推荐说明均有 hover `title` 展示完整内容。
42. v5 新增：账号分组 chip 改为单行可滚动区域；每个分组保留 `X`，点击后打开“确认分组组合”弹窗，至少保留一个分组时移除按钮禁用。900px 窄窗口下页面无横向溢出，滚动只发生在分组 chip 区域内。
43. v6 新增：基于演示截图复测聚焦账号卡，DOM 读数确认不在 `.station-account-focus` 内展示站点余额；来源/成本状态 badge 高度为 24px、圆角为 `999px`，可见文本仅为 `未绑来源` / `成本未设`，详情只在 `title` / `aria-label` 中。
44. v6 新增：聚焦账号分组条作为整行区域显示，`grid-column` 为 `1 / -1`；示例两个分组 chip 各约 410px 宽且带移除按钮。点击 `从 claude-main 移除 Claude Code Pro` 会打开“确认分组组合”弹窗，弹窗说明“确认后才会提交到远程”，随后已取消未提交。
45. 成本保护安全推荐 v1 新增：本轮通过代码、单测、生产构建、macOS 打包、严格签名和差异审查验证；未替用户打开桌面 App。用户可从桌面快捷入口进入后重点检查：价格榜/来源钱包是否显示专属/订阅/峰值标签，我的站点账号是否出现“自动来源”，0.02 账号是否只推荐不低于 0.02 的安全候选，成本保护四个统计卡是否可点击筛选。
46. v6.1 新增：账号详情改为行内展开，站点标题不再自动展开首个账号；价格榜最终倍率旁显示涨跌图标，涨价时应用内 warning toast 提醒查看分组变化。
46. lcodex 授权修复 v1 新增：只读检查确认 `https://lcodex.cc/login` 页面公开配置包含 `api_base_url: https://api.lcodex.cc`，前端会使用 `sub2api_auth_client_id` 和 `/auth/session/restore` 恢复 session。桌面端授权窗口已改为读取这些公开字段并调用 restore；本轮未替用户输入 lcodex 账号密码，需用户实测登录后弹窗是否自动关闭。

## 截图证据

- `screenshots/main-dashboard.jpeg`
- `screenshots/station-settings.jpeg`
- `screenshots/group-mutation-confirm.jpeg`
- `screenshots/bubble-window.jpeg`
- `screenshots/account-focus-panel.png`
- `screenshots/account-candidate-panel.png`

## 未覆盖

- 未使用真实生产站点执行账号切组，避免产生未授权远程写入。
- 浏览器预览只验证本地交互和内存态；桌面端授权桥接、重定向兼容、cookie/UA 会话携带与错误按钮语义已验证存在，但真实站点网页登录仍需用户在授权窗口完成。
- v7 未通过可见自动化截图逐项点击；本轮已完成 TypeScript、单元测试、生产构建、macOS 打包签名和桌面 app 启动，真实数据下的 tabs/低价聚合需要用户站点返回 `/channels/available` 后验收。
- v8 未接入真实站点价格样本做截图验收；当前自动验证确认构建、测试和打包通过，真实排序效果需要用户站点返回分组与 `/channels/available` 模型价格后验收。
- v3-2 的排序验证使用本地浏览器预览页面和演示数据完成；`package:mac` 本轮遇到外部 Electron 下载 `ECONNRESET`，因此未重新出包，但 renderer 交互和截图已确认排序头可用。
- v3-2 紧凑模式的排序条验证使用 `?mode=compact` 预览和 840px/600px 两个宽度完成；`package:mac` 与严格签名校验本轮已通过。
- v4-1 的最终倍率通过单测、TypeScript、生产构建和 macOS 打包验证；真实站点样本下的采购排序偏好仍待用户确认。
- v4-2 的管理台 tabs 修复已通过代码、单测、构建和打包验证；当前真机可见复测被锁屏打断，需用户解锁 Mac 后再补一轮桌面截图。
- v4-4/v4-5/v4-6 的账号组合语义、聚焦面板与更优候选池已通过代码、单测、构建、打包、Electron CDP 可见路径和演示数据截图验证；真实多分组账号的组合编辑仍需用户站点联调，避免对生产站点发起未授权远程写入。
- v4-7 的预算线过滤已通过代码、单测、构建、打包和隔离 CDP DOM 读数验证；当前未额外补截图，因为 CDP 截图在隔离窗口上超时。
- v4-8 已通过代码、单测、构建、打包和 `git diff --check`；可见重开未在本轮完成，因为用户要求自己打开验证。
- v4-9 已通过代码、单测、构建、打包、严格签名校验和 `git diff --check`；本轮继续遵守用户“我自己打开验证”的要求，未重新启动桌面窗口。
- v4-9 补充审计未做可见点击，只做正式数据文件计数和进程列表核对；未打印敏感配置或原始站点数据。
- v4-10 数据中心已通过代码、单测、生产构建、macOS 打包、严格签名校验和 `git diff --check`；本轮默认不启动桌面 app，避免重新制造多实例。数据中心摘要由主进程返回脱敏计数和文件元数据，未打印或展示 token、cookie、原始站点配置、账号明细。
- v4-11 聪明哥网络栈修复已通过代码、单测、生产构建、macOS 打包、严格签名校验、`git diff --check` 和无窗口只读站点探针；本轮未打开桌面窗口，避免多实例。探针只输出状态码、content-type 和脱敏 message，不打印 token、cookie、原始响应或站点配置。
- v4-14 账号调度筛选与最低卡片直显已通过代码、单测、生产构建、macOS 打包、严格签名校验、本地浏览器预览和产物文本/CSS 抽查；本轮没有打开桌面 App，避免产生多个应用实例。
- v4-15 账号列表可读与操作增强已通过代码、单测、生产构建、macOS 打包、严格签名校验、本地浏览器预览和产物文本/CSS 抽查；本轮没有打开桌面 App，也没有对生产站点提交移除分组。
- v5 账号工作台信息密度与分组移除交互已通过 `npm run verify`、`git diff --check` 和本地浏览器预览；本轮没有重新打开桌面 App，避免产生多个实例，也没有对生产站点提交移除分组。
- v6 账号工作台对齐截图复修已通过本地浏览器预览、900px 窄屏、`npm run verify`、`npm run package:mac`、严格签名校验和产物 CSS/JS 抽查；本轮没有替用户启动桌面 App，也没有对生产站点提交移除分组。
- 成本保护安全推荐 v1 已通过 `npm run typecheck`、目标测试、`npm run verify`、`npm run package:mac`、严格签名校验和 `git diff --check`；本轮没有替用户启动桌面 App，避免产生多个实例；真实站点自动来源命中、专属分组字段展示和成本卡点击筛选需用户在最新桌面包里确认。
- lcodex 授权修复 v1 已通过 `npm run typecheck`、`npm test -- tests/web-auth.test.ts tests/sub2api-client.test.ts tests/storage.test.ts`、`npm run verify`、`npm run package:mac`、严格签名校验和 `git diff --check`；本轮没有替用户登录真实 lcodex 账号，因此真实网页登录自动关闭仍需用户自己在桌面 App 中确认。
- 未验证 macOS 签名、公证、自动更新和安装包升级。
- Tray 的创建与点击逻辑已运行，但不同 macOS 菜单栏布局仍需在最终发布包中复测。
- v3-1 兼容诊断器与手动补录字段已通过代码、单测、构建、打包和严格签名校验；本轮没有重新打开桌面窗口做可见回归，因此设置面板的新诊断区域仍建议在下一次桌面联调中确认布局。

## 站点类型适配器 正式实施包 v1

1. 浏览器预览从“添加真实站点”打开设置窗口，默认站点类型显示 `自动检测`，并与“站点角色”独立。
2. 切换为 `NewAPI` 后，地址说明切换为站点根地址；路径区域仅保留 `/api/user/self`、`/api/token/?p=0&size=100`、`/api/models`，同时显示“不读取 Sub2API 的 /groups 或 /admin 路径”和“不进入价格榜”的能力边界。
3. 在 `900 x 760` 约束窗口检查页面 `scrollWidth === clientWidth === 900`；模态框使用自身纵向滚动，未出现页面横向溢出或控件重叠。
4. 本轮预览只使用内存演示数据，没有发起网页授权、站点刷新或远程写入；预览服务在检查后已停止。
- v11 新增：启动阶段不再主动解密旧 token，因此不会立即弹出 `AIZZZWatch Safe Storage` 钥匙串授权；桌面截图确认主窗口可见，旧站点显示“暂无数据”，等待用户手动刷新/重新登录。
- 账号上游密钥关联 v1：最终 macOS 包已实际启动并进入 `我的站点 → 账号`。账号卡显示“关联上游密钥”，来源状态文案为“未关联”；选择已返回 Key 列表的三方站点后，弹窗展示“上游密钥”下拉和“保护口径分组”。未选 Key 时口径控件禁用并提示先选 Key；未保存任何关联，也未调用远程写入。
# 收益区间核算 正式实施包 v1

- 路径：构建后的 renderer 预览 → `我的站点` → `收益`。
- 已验证：收益标签可达；默认空态清楚说明不会展示原始明细；按天/按小时切换可用；开始/结束日期、账号和售卖分组筛选可见；按小时显示 7 天上限。
- 窄窗口：900px 浏览器视口下页面 `scrollWidth === clientWidth === 900`，收益面板和筛选栏未出现水平溢出或重叠。
- 限制：演示模式不会执行管理员用量请求；真实管理员数据的完整/页数上限/不可用状态由用户站点实际返回后继续验收。

# 上游使用状态 v2

- 启动最新签名包后，价格榜的“倍率变动”同一行可见紧凑开关。关闭状态展示 `未使用`，价格榜显示 142 条未精确关联的三方分组；开启后文案变为 `使用中`，并展示“当前筛选下没有已精确关联到我的站点账号的三方分组”的空态。
- AX 状态同步：关闭时 `aria-label` 为“正在显示我的站点未使用的分组”；开启时为“正在显示我的站点使用中的分组”，并给出当前匹配数量。两次点击均立即更新列表和空态。
- 只读诊断：切换“来源钱包 → 我的站点”后，若自有聚合站会话失效，账号快照为空且“重建来源”禁用；界面会提示重新授权。没有执行刷新、重新授权、管理员修改或其他远程写入。

# 价格榜最近涨降时间 思考确认 v1（已由 v2 替代）

- 路径：最新签名包 → `查价视图` → `价格榜`。
- 已验证：最终倍率下方在同一单元格内显示两条紧凑状态。没有历史的行显示 `↑ --` 和 `↓ --`；搜索已有记录 `A004-K12/BugTeam` 后显示 `↑ 07-22 02:20`、`↓ --`。
- 悬停/辅助文本：涨价时间携带“最近涨价、0.020x → 0.030x、完整本地观察时间”；降价无记录时说明“暂无本地观察到的降价”。
- 布局：价格榜行高调整为 76px，没有新增表格列；可见验证未发现横向溢出或内容重叠。
- 边界：本轮未点击刷新、重新授权、授权登录或任何管理员写入操作。

# 价格榜最近涨降时间 思考确认 v2

- 预期路径：最新签名包 → `查价视图` → `价格榜`。
- 状态：最终倍率下只渲染当前最新涨价或降价的一条完整时间。涨价为红色 `涨价 · YYYY-MM-DD HH:mm:ss`，降价为绿色同格式；无涨跌事件没有时间行。
- 静态布局检查：时间元素不使用 `text-overflow: ellipsis` 或 `nowrap`；窄列在日期与时间间允许换行，不会丢失秒数。
- 自动化限制：本地构建文件 URL 被浏览器安全策略拒绝，按策略未用其他浏览器或自动化路径绕过。因此新版的实际像素布局仍待用户打开最新桌面包验收。

# 自有账号免计费 正式实施包 v1

- 预期路径：最新签名包 → `我的站点` → `成本` → 选择账号的“设置账号成本” → 成本类型选择“自有免计费” → 保存；再进入 `收益` 查看区间汇总。
- 预期状态：成本列表出现“免计费”状态和“自有免计费”筛选项；该账号不出现在亏损、接近亏损、未绑定来源或基础倍率需调的结果中；若分组内仅有免计费账号，分组卡显示“免计费账号不参与成本比较”。
- 收益状态：收入与请求保留；“免计费口径”显示对应金额/请求，账号/时间行的毛利显示“含免计费”；上游成本和账号成本不累计该账号。
- 自动化限制：本轮本机浏览器无法访问 Vite 本地端口，按浏览器策略未切换其他自动化路径绕过。上述路径需用户在最新版桌面包中完成一次可见验收。

# 用户自用排除经营核算 正式实施包 v1

- 预期路径：最新签名包 → `我的站点` → `用户` → 选中你自己创建并使用的用户 → `设为内部自用`；再进入 `收益`，点击图标化“重新归档当前区间”后查看汇总。
- 预期状态：用户按钮切换为绿色“内部自用”；收益经营汇总不再包含该用户的收入、成本、毛利或亏损；下方出现“内部自用消耗”并显示请求数、站内扣费参考与可追溯上游成本。
- 边界状态：用户列表未返回数字用户 ID 时显示“无法识别用户 ID”；收益记录未带 `user_id` 时显示黄色提示，明确这些记录未被排除。取消内部自用后，重新归档即可恢复经营核算。
- 自动化限制：构建后本地文件 URL 被浏览器安全策略拒绝，未使用替代浏览器路径绕过。桌面端可见点击和真实管理员用量字段由用户在最新包中验收。

# 接口适配中心 正式实施包 v1

- 预期路径：最新签名包 → 顶部 `数据接入` → 选择三方站点 → 填写接口路径与点路径字段 → `检测并预览` → `保存适配`。
- 成功状态：每个能力显示 `可用 / 待补字段 / 不可用`；检测结果只显示记录数、路径和摘要。保存后自动刷新该站点，来源钱包/价格榜随映射结果更新。
- 边界状态：输入数组表达式、JSONPath、脚本或超过 12 段的路径后保存，非法字段会被丢弃；自定义映射遇到 HTTP 或跨域完整 URL 会被拒绝；NewAPI 显示内置只读适配说明。
- 自动化限制：本机浏览器策略拒绝打开构建文件 URL，未使用其他浏览器或绕过方式。请在 `release/AIZZZWatch-darwin-arm64/AIZZZWatch.app` 中完成一次可见验收。
